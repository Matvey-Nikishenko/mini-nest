{
  "name": "mini-nest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "mini-Nest: IoC + HTTP + request lifecycle (part 3)",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/src/server.js",
    "test": "tsc && node --test dist/test/*.test.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "reflect-metadata": "^0.2.2",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^22.19.2",
    "typescript": "~6.0.3"
  }
}
