{
  "name": "mini-nest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "mini-Nest: IoC + HTTP на node:http (part 2)",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/src/server.js",
    "test": "tsc && node --test dist/test/*.test.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "class-transformer": "^0.5.1",
    "class-validator": "^0.15.1",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@types/node": "^22.19.2",
    "typescript": "~6.0.3"
  }
}
